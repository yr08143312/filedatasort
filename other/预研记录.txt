com.itheima    包名-全部小写（黑马英文名）
数据实体类：FileDataVo
资源类（线程争抢的资源）：Resources
设计模式：考虑之后可能有其他的排序规则，所以我们使用策略模式。

总结中，需要记录需要注意什么？重点难点在哪？用到了什么数据结构。用到了什么设计模式。

重点：
1、数据结构方面：我们使用了LinkedList,因为会频繁插入数据
1、线程之间有争抢的发生，所以一定有一个被争抢的资源类。对应该例中的Resources。
2、线程之间的协作，使用wait()和notify()/notifyAll()方法。这三个方法都是继承自object类的。
	wait()方法：使当前用有锁的线程等待，并释放锁。
	notify()/notifyAll()：唤醒等待的线程。但线程并不是立即执行，而是使线程加入到对cpu的争抢中来。
	wait()方法要放在循环中
3、设计模式方面，我们使用了策略模式，主要是考虑到拓展，因为以后可能出现另外一种排序算法的出现。如果需求说要提供其他的算法，那么我们只需要继承--?????
